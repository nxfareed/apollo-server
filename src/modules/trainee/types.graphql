input CreateUserInput {
  name: String!
  email: String!
  password: String!
}

input UpdateUserInput {
  id: ID!
  password: String
  name: String
  email: String
}

input getUserData {
  skip: Int
  limit: Int
}

type getData {
  records: [User]
  count: Int
}

type CreateUserOutput {
  name: String!
  email: String!
  role: String!
}
